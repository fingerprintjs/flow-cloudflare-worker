{
  "name": "worker-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "pnpm lint --fix",
    "deploy": "wrangler deploy",
    "wrangler:types": "wrangler types --strict-vars=false"
  }
}
